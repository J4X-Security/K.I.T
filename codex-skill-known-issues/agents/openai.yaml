interface:
  display_name: "Known Issues Aggregator"
  short_description: "Build, extend, and query a canonical known-issues register from audit reports"
  default_prompt: "Use $known-issues-aggregator to consolidate audit reports into known-issues.md, extend an existing known-issues register, or check whether a newly reported issue is already known."
